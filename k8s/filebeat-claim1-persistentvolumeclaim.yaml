apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: filebeat-claim1
  name: filebeat-claim1
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
