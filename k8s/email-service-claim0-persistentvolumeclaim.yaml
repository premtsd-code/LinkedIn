apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: email-service-claim0
  name: email-service-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
