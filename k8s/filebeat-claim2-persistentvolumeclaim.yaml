apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: filebeat-claim2
  name: filebeat-claim2
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
