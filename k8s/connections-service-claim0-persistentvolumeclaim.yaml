apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: connections-service-claim0
  name: connections-service-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
