apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: linkedin-db-data
  name: linkedin-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
