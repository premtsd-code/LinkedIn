apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: connections-db-data
  name: connections-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
